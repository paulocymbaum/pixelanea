# Canonical skill-output smoke sources for CI (scripts/run-skill-output-smoke.sh).
# One entry per line: TYPE<TAB>PATH
#   test-md  — run ## Automated bash blocks from test.md
#   matrix   — fail if any case Status is [ ] or [!]
#
# Paths must be tracked in git (not under the gitignored .cursor/skill-outputs/ tree).
# Update when a delivery becomes the regression anchor for a feature area.

test-md	.cursor/ci-smoke/harness-gate/test.md
matrix	.cursor/ci-smoke/paint-matrix-anchor/test_matrix_unit.md
